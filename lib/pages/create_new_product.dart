import 'package:flutter/material.dart';

class CreateNewProduct extends StatelessWidget {
  const CreateNewProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Create new porduct .'),
      ),
    );
  }
}
